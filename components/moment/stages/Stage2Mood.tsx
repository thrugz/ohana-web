import type { StageProps } from "@/lib/moment/types"

// Phase 0 stub. Real Stage 2 (Mood Match) lands in Phase 2.
export function Stage2Mood(_: StageProps) {
  return <div data-testid="stage-2" />
}
