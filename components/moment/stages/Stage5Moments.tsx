import type { StageProps } from "@/lib/moment/types"

// Phase 0 stub. Real Stage 5 (Moments bridge) lands in Phase 5.
export function Stage5Moments(_: StageProps) {
  return <div data-testid="stage-5" />
}
