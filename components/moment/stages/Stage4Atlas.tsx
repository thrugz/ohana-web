import type { StageProps } from "@/lib/moment/types"

// Phase 0 stub. Real Stage 4 (Moods Atlas / The Wanderer) lands in Phase 4.
export function Stage4Atlas(_: StageProps) {
  return <div data-testid="stage-4" />
}
