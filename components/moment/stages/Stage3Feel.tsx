import type { StageProps } from "@/lib/moment/types"

// Phase 0 stub. Real Stage 3 (cluster cards) lands in Phase 3.
export function Stage3Feel(_: StageProps) {
  return <div data-testid="stage-3" />
}
